services:
  app:
    build: .
    image: gdns
    command: /docker-gdns
    ports:
      - "8081:8080"
    environment:
      - REDIS_URL=redis:6379
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6378:6379"
